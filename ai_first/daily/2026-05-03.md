## AUTH_MULTI_USER_FOUNDATION Continuation

- Branch: `fix/auth-multi-user-foundation`
- Task: `AUTH_MULTI_USER_FOUNDATION`
- Done: Added `deeptutor/services/auth/mailer.py` as a bounded SMTP auth mailer seam with message builders for password reset and email verification plus TLS/SSL-aware delivery settings loaded from `DEEPTUTOR_AUTH_SMTP_*` and `DEEPTUTOR_AUTH_FROM_*`.
- Done: Wired `POST /api/v1/auth/forgot-password` and `POST /api/v1/auth/send-verification` to deliver real reset/verify emails when SMTP is configured, while preserving privacy-safe generic responses and the existing explicit debug-token fallback for local/test flows.
- Done: Tightened authorization depth inside the owned `sessions` router by enforcing `current_user.id` ownership on `assessment-review`, `assessment-rubric-review`, and `quiz-results` endpoints instead of trusting only the `session_id`.
- Done: Replaced the hardcoded demo cookie settings with env-driven auth cookie controls for `secure`, `samesite`, and `max-age`, then covered the secure-cookie contract in router tests.
- Done: Preserved safe `next` redirects across email/password and Google auth flows, and removed the login-page Google hardcode to `student` by letting the user choose the first-login Google role in the auth UI.
- Done: Added a shared non-blocking email-verification banner across signed-in teacher/student/admin shells and the legacy teacher-first surfaces, with resend-email and refresh-status actions so verification is now visible in normal product use.
- Done: Added a shared signed-in account bar across the owned auth shells and teacher-first legacy surfaces so role, email, verification state, and logout are always visible after login.
- Done: Extended the admin roster into a lifecycle surface with role/status updates, and enforced `status=active` across password login, existing session reuse, and Google callback so suspended accounts are no longer cosmetic-only.
- Done: Expanded backend coverage with `tests/services/auth/test_mailer.py`, SMTP router-delivery assertions in `tests/api/test_auth_router.py`, and ownership regression coverage in `tests/api/test_session_review_router.py`.
- Done: Ran a full review pass on the auth lane, wrote `code_review_report_fix-auth-multi-user-foundation.md`, fixed admin self-lockout, fixed stale failed-save state in the admin roster UI, and cached one-time auth schema initialization so request-scoped `AuthService()` no longer repeats bootstrap work unnecessarily.
- Done: Hardened the auth mail seam with explicit delivery policy modes (`auto`, `disabled`, `required`), optional reply-to/provider metadata, mutual-exclusion validation for TLS vs SSL, and required-mode behavior that now returns `503` for signed-in verification sends when production mail delivery is not configured.
- Done: Preserved privacy-safe forgot-password semantics even when required mail delivery is misconfigured by keeping the public response generic while logging the delivery/config failure server-side.
- Done: Expanded auth hardening into backend teacher-first routers: `dashboard`, `knowledge`, and `marketplace` now require authenticated `teacher` or `admin` access, while the `knowledge` health/config sync maintenance surfaces are admin-gated.
- Done: Scoped dashboard reads to owned sessions for teachers, added owner-bound evidence rows for teacher actions, recommendation acknowledgements/feedback, overrides, diagnosis feedback, and intervention assignments, and covered owner-scoped updates in `tests/api/test_dashboard_router.py`.
- Done: Stamped auth-era knowledge packs with `owner_user_id` metadata, made `/api/v1/knowledge/default` user-specific instead of global, and changed marketplace imports to create per-user imported copies (`<pack>__imported__<user_id>`) so teachers no longer collide on shared import names.
- Done: Rebuilt `observations` and `student_states` in the SQLite learning-session store onto composite owner-aware keys, so repeated `student_id` or `observation_id` values across teachers no longer overwrite or bleed into one another after auth rollout.
- Done: Threaded `owner_user_id` through dashboard insight materialization for observations and student-state rollups, then added regression coverage proving a second teacher's follow-up observations do not mutate the first teacher's scoped evidence set.
- Done: Threaded the same owner scope through tutoring turn runtime and context building, so live chat follow-up observations and stored student-state context now stay attached to the session owner instead of falling back to the anonymous bucket after login.
- Done: Hardened `assessment.py` with the same teacher/admin auth gate and owner scope, so assessment recommendation and diagnosis now only read the caller's owned sessions and support signals, with new router tests for unauthenticated access, cross-owner denial, and owner-only recommendation history.
- Done: Bound `/api/v1/ws` to the authenticated websocket cookie and propagated owner scope through start/subscribe/cancel flows, so the primary chat transport can no longer read or mutate another user's turns by session id alone.
- Done: Moved lightweight `SUMMARY.md` / `PROFILE.md` memory into owner-specific storage, auth-gated `/api/v1/memory`, and bound runtime memory context plus refresh writes to the authenticated actor instead of one global shared file pair.
- Done: Extended the legacy `BaseSessionManager` contract with optional `owner_user_id`, then hardened `/api/v1/chat*` and `/api/v1/solve*` so their REST and websocket entrypoints now require auth and filter JSON-backed legacy sessions by owner.
- Done: Hardened `/api/v1/guide*` so REST and websocket flows now require auth, guided-learning session files persist `owner_user_id`, and all create/get/list/chat/page/reset/delete flows resolve only the authenticated owner's sessions unless the caller is admin.
- Done: Hardened `/api/v1/tutorbot*` so teacher/admin auth is required for souls, bot lifecycle, bot files, history, and websocket chat, while newly created bot configs now persist owner metadata and normal teachers only see their own bots.
- Done: Hardened `/api/v1/notebook*` so notebook list/detail/update/delete/add-record flows now require auth, notebook files/index persist owner metadata, and non-admin users only resolve notebooks they own.
- Done: Moved UI settings off the shared `interface.json` file into per-user settings files, while restricting settings catalog mutation/apply/test/tour flows to admins.
- Done: Gated `/api/v1/agent-specs*` behind authenticated teacher/admin access so authoring and runtime-policy audit surfaces are no longer reachable by students or anonymous users.
- Done: Hardened the remaining runtime/operator routers: `question` websocket generation now requires teacher/admin auth, `vision_solver` REST/websocket analysis requires auth, `plugins_api` execution is admin-only, `system` pilot-feedback and runtime tests are role-gated, and `agent_config` is no longer public.
- Done: Added owner metadata to co-writer history/tool-call artifacts and filtered co-writer history lookups by authenticated owner, so writing traces no longer leak across accounts.
- Done: Ran a whole-branch self-review against `origin/main`, rewrote `code_review_report_fix-auth-multi-user-foundation.md`, and fixed a Google OAuth login-CSRF gap by binding `/api/v1/auth/google/start` and `/google/callback` with an HttpOnly nonce cookie that must round-trip through `state`.
- Done: Fixed legacy chat/solve session retention so `BaseSessionManager.MAX_SESSIONS` now trims only the current owner's sessions instead of evicting unrelated owners from the shared JSON file.
- Risks: legacy ownerless knowledge-base records are still visible to non-admin teachers for backward compatibility; a stricter migration path can hide them later once existing demo data and production seed data are re-owned.
- Risks: legacy ownerless observation/state rows remain isolated under the empty-owner bucket and will not surface in authenticated teacher dashboards unless explicitly migrated or re-materialized under a real owner.
- Risks: tutorbot ownership is enforced at the router/config layer for this pass; deeper multi-tenant isolation of every tutorbot internal artifact and background integration path can still be tightened later if the bot surface becomes production-critical.
- Risks: pre-auth notebook files without `owner_user_id` are now intentionally hidden from normal users for safety; if that historical data matters, it will need an explicit migration or admin-side reassignment step later.
- Risks: `question` currently gained auth/role gates but not a separate owner-addressable history surface because its router is still primarily websocket generation without user-visible per-owner retrieval endpoints; if a question-history UI appears later, that surface will need a dedicated ownership pass.
- Tests: `python -m pytest tests/services/auth/test_mailer.py tests/api/test_auth_router.py -v`
- Tests: `python -m pytest tests/api/test_session_review_router.py tests/services/auth tests/api/test_auth_router.py tests/api/test_admin_users_router.py tests/services/session/test_owned_session_store.py -v`
- Tests: `python -m pytest tests/services/auth tests/api/test_auth_router.py tests/api/test_admin_users_router.py tests/services/session/test_owned_session_store.py tests/api/test_session_review_router.py tests/api/test_dashboard_router.py tests/api/test_marketplace_router.py tests/api/test_knowledge_router.py -v`
- Tests: `python -m pytest tests/services/session/test_sqlite_store.py tests/api/test_dashboard_router.py -v`
- Tests: `python -m pytest tests/api/test_unified_ws_turn_runtime.py tests/services/session/test_turn_runtime_helpers.py tests/services/session/test_sqlite_store.py -v`
- Tests: `python -m pytest tests/api/test_assessment_router.py -v`
- Tests: `python -m pytest tests/services/auth tests/api/test_auth_router.py tests/api/test_admin_users_router.py tests/services/session/test_owned_session_store.py tests/services/session/test_sqlite_store.py tests/services/session/test_turn_runtime_helpers.py tests/api/test_unified_ws_turn_runtime.py tests/api/test_session_review_router.py tests/api/test_dashboard_router.py tests/api/test_marketplace_router.py tests/api/test_knowledge_router.py tests/api/test_assessment_router.py -v`
- Tests: `python -m pytest tests/services/memory/test_memory_service.py tests/api/test_memory_router.py tests/api/test_unified_ws_turn_runtime.py -v`
- Tests: `python -m pytest tests/api/test_chat_router.py tests/api/test_solve_router.py -v`
- Tests: `python -m pytest tests/services/auth tests/services/memory/test_memory_service.py tests/api/test_auth_router.py tests/api/test_admin_users_router.py tests/api/test_memory_router.py tests/services/session/test_owned_session_store.py tests/services/session/test_sqlite_store.py tests/services/session/test_turn_runtime_helpers.py tests/api/test_unified_ws_turn_runtime.py tests/api/test_session_review_router.py tests/api/test_dashboard_router.py tests/api/test_marketplace_router.py tests/api/test_knowledge_router.py tests/api/test_assessment_router.py tests/api/test_chat_router.py tests/api/test_solve_router.py -v`
- Tests: `cd web && node node_modules/vitest/vitest.mjs run tests/auth-admin-page.test.tsx tests/auth-signed-in-account-bar.test.tsx tests/auth-email-verification-banner.test.tsx tests/auth-teacher-surface-gate.test.tsx tests/auth-shell-layout-source.test.ts`
- Tests: `cd web && npm run build`
- Tests: `git diff --check`
- Tests: `python -m pytest tests/api/test_guide_router.py tests/api/test_tutorbot_router.py -v`
- Tests: `python -m pytest tests/services/auth tests/services/memory/test_memory_service.py tests/api/test_auth_router.py tests/api/test_admin_users_router.py tests/api/test_memory_router.py tests/services/session/test_owned_session_store.py tests/services/session/test_sqlite_store.py tests/services/session/test_turn_runtime_helpers.py tests/api/test_unified_ws_turn_runtime.py tests/api/test_session_review_router.py tests/api/test_dashboard_router.py tests/api/test_marketplace_router.py tests/api/test_knowledge_router.py tests/api/test_assessment_router.py tests/api/test_chat_router.py tests/api/test_solve_router.py tests/api/test_guide_router.py tests/api/test_tutorbot_router.py -v`
- Tests: `python -m pytest tests/api/test_notebook_router.py tests/api/test_settings_router.py tests/api/test_agent_specs_router.py tests/services/notebook/test_service_owner_scope.py -v`
- Tests: `python -m pytest tests/api/test_question_router.py tests/api/test_co_writer_router.py tests/api/test_system_router.py tests/api/test_vision_solver_router.py tests/api/test_plugins_api_router.py tests/api/test_agent_config_router.py -v`
- Tests: `python -m pytest tests/api/test_auth_router.py tests/services/session/test_base_session_manager.py -q`
- Tests: `python -m pytest tests/services/auth tests/services/session/test_base_session_manager.py tests/services/session/test_owned_session_store.py tests/services/session/test_sqlite_store.py tests/api/test_auth_router.py tests/api/test_chat_router.py tests/api/test_solve_router.py tests/api/test_unified_ws_turn_runtime.py tests/api/test_memory_router.py tests/api/test_dashboard_router.py tests/api/test_marketplace_router.py tests/api/test_knowledge_router.py tests/api/test_assessment_router.py tests/api/test_guide_router.py tests/api/test_tutorbot_router.py tests/api/test_notebook_router.py tests/api/test_settings_router.py tests/api/test_agent_specs_router.py tests/api/test_question_router.py tests/api/test_co_writer_router.py tests/api/test_system_router.py tests/api/test_vision_solver_router.py tests/api/test_plugins_api_router.py tests/api/test_agent_config_router.py tests/api/test_admin_users_router.py tests/api/test_session_review_router.py -q`
- Tests: `cd web && node node_modules/vitest/vitest.mjs run tests/auth-login-page.test.tsx tests/auth-role-picker.test.tsx tests/auth-signup-page.test.tsx tests/role-shell-routing.test.tsx tests/auth-recovery-pages.test.tsx tests/auth-verify-page.test.tsx tests/auth-teacher-surface-gate.test.tsx tests/auth-shell-layout-source.test.ts tests/auth-admin-page.test.tsx tests/auth-role-hubs.test.tsx tests/auth-signed-in-account-bar.test.tsx tests/auth-email-verification-banner.test.tsx`
