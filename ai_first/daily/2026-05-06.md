# 2026-05-06

## Frontend auth cookie hardening lane

- Root-caused the reported post-login Knowledge upload `401`: backend protected routers now trust only the `deeptutor_session` cookie, while multiple legacy teacher-first frontend fetches still omit `credentials: "include"`.
- Confirmed the bug is systemic, not isolated to Knowledge. Affected protected surfaces include Knowledge, Dashboard helpers, Marketplace helpers, Notebook helpers, Memory, Settings, Guide session hooks, TutorBot/Agents pages, and notebook-save flows.
- Opened bounded worktree `/Users/nguyenhuuloc/Documents/Multiagent-learning-platform/.worktrees/fix-frontend-auth-cookie-hardening` on branch `fix/frontend-auth-cookie-hardening`.
- Wrote spec, task packet, and implementation plan for a shared protected-fetch hardening pass plus regression guards to prevent future raw protected fetches.
- Implemented `apiFetch(...)` in `web/lib/api.ts` to default protected internal API calls to `credentials: "include"`.
- Migrated protected teacher-first helper modules and direct page/component fetches across Knowledge, Dashboard, Marketplace, Notebook, Agent Spec, Memory, Settings, Guide, Tutorbot/Agents, Co-writer, and Playground to the shared helper.
- Tightened `web/lib/knowledge-api.ts` so HTTP auth failures are no longer masked as empty/offline state; only genuine network failures fall back to cached imported packs.
- Added regression tests for the new helper, Knowledge auth behavior, and a source invariant that blocks raw protected `fetch(apiUrl(...))` usage from returning on the hardened surfaces.
- Verification passed with:
  - focused Vitest auth-cookie hardening tests
  - `cd web && npm run build`
  - `cd web && npm run test:coverage:frontend`
  - `git diff --check`
