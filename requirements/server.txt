# ============================================
# DeepTutor Server — Full Dependencies (CLI + Web)
# ============================================
# Install: pip install -r requirements/server.txt && pip install -e .
# Everything needed to run the API server and web frontend.

-r cli.txt

# --- API server ---
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
sqlalchemy>=2.0.41
psycopg[binary]>=3.2.9
alembic>=1.16.4
authlib>=1.6.0
argon2-cffi>=25.1.0
